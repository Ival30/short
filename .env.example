# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Python Backend URL
# CRITICAL: Must match your backend deployment URL
# Development: http://localhost:8000
# Production: https://your-backend-url.com (MUST be HTTPS)
VITE_BACKEND_URL=http://localhost:8000

# ⚠️ CORS and Mixed Content Issues:
# - If frontend is HTTPS, backend MUST also be HTTPS
# - Mixed content errors = protocol mismatch (HTTP vs HTTPS)
# - Local dev: Use HTTP for both
# - Production: Use HTTPS for both

# Production Examples:
# VITE_BACKEND_URL=https://clipforge-backend.railway.app
# VITE_BACKEND_URL=https://clipforge-api.render.com
# VITE_BACKEND_URL=https://api.clipforge.com
